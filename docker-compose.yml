services:
  router:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      ROUTER_ENV: production
      HOST: 0.0.0.0
      PORT: 8080
      CACHE_MODE: memory
    restart: unless-stopped
